export interface UserTheme {
  userId: string;
  themeMode: string;
  primaryColor: string;
  secondaryColor: string;
  primaryFontColor: string;
  secondaryFontColor: string;
  accentColor: string;
}
